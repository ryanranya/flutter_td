import 'package:flutter/material.dart';

class TDMinePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('我的'),
      ),
      body: Text("我的页展示"),
    );;
  }
}
